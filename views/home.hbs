<div class="container">
  <div class="row">
    <div class="col">
      <p class="text-center">Want To See Something Cool...</p>
      <p class="text-center phone_text">Phone Number: (917) 746-0378</p>
      <p class="text-center phone_text">Send a photo of yourself happy or sad</p>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="col">
    </div>
    <div class="col-3">
      <form method="POST" action="/api/parseImageBase64">

      <input id="filePicker" type="file" name="image" accept="image/*" onchange="upload()">


      <p id="base64Display"></p>
      <label for="browse">Upload</label>
      <input type="submit" name="submit" value="Submit">
      </form>
    </div>
    function upload(){

var file = document.querySelector('#filePircke> input[type="file"]').files[0];
getBase64(file);

    }
    </script> -->
    <script>
    var handleFileSelect = function(evt) {
  var files = evt.target.files;
  var file = files[0];

  if (files && file) {
      var reader = new FileReader();

      reader.onload = function(readerEvt) {
          var binaryString = readerEvt.target.result;
          console.log(btoa(binaryString));
      };

      reader.readAsBinaryString(file);
  }
};

if (window.File && window.FileReader && window.FileList && window.Blob) {
  document.getElementById('filePicker').addEventListener('change', handleFileSelect, false);
} else {
  alert('The File APIs are not fully supported in this browser.');
}
</script>


    <div class="col">
    </div>
  </div>
</div>
